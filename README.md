# chatapp

## Python version
3.7.3


## Installation
Depenencies can be installed using pip via `requirements.txt` file. 

To install run 
```
pip install -r requirements.txt
```

## Running
Using python executable run `manage.py` with argument `runserver`
```
python manage.py runserver
```
